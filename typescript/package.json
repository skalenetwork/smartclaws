{
  "name": "@skalenetwork/smartclaws-cli",
  "version": "0.1.0",
  "type": "module",
  "license": "LGPL-3.0-or-later",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile src/index.ts --outfile dist/smartclaws",
    "check": "tsc --noEmit",
    "lint": "biome check src/",
    "lint:fix": "biome check --fix src/",
    "test": "bun test",
    "test:unit": "bun test tests/envelope.test.ts tests/config.test.ts tests/wallet.test.ts",
    "test:integration": "bun test tests/channel.test.ts"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "viem": "^2.47.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.0"
  }
}
