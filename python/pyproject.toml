[project]
name = "smartclaws"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["bleak"]

[dependency-groups]
dev = ["ruff"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
